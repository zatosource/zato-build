---

    - hosts: "{{ repo_host }}"
      become: True
      vars:
        repo_datetime: "{{ ansible_date_time.iso8601 }}"
      vars_files:
          - vars/vars.yml
          - vars/common.yml
          - vars/build-box.yml

      roles:

          - add_package_to_repo
