---

    - hosts: "{{ host }}"
      become: True
      become_user: aptly
      vars:
        repo_datetime: "{{ ansible_date_time.iso8601 }}"
      vars_files:
          - vars/vars.yml
          - vars/parameters.yml

      roles:

          - add_package_to_repo
