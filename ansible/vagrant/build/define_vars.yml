---

- hosts: localhost

  tasks:

    - name: Prepare box vars file
      template: src=./templates/box_vars.j2
                dest=./vars/build-box.yml

    - name: Prepare package vars file
      template: src=./templates/package.j2
                dest=./vars/package.yml

    - name: Prepare repository vars file
      template: src=./templates/repository.j2
                dest=./vars/repository.yml
