---

  - hosts: '{{ target }}'
    become: True

    roles:

        - install_additional_soft
        - install_redis
        - install_postgresql
        - configure_postgresql
        - install_zato
        - create_quickstart_postgresql
        - test_quickstart
