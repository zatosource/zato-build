---

- name: Copy a Zato package to a deb repo-box
  copy: src=../../../output/{{ system }}/zato-{{ release_version }}-{{ package_version }}_{{ architecture }}-{{ codename }}.{{ format }}
         dest=/opt/aptly/incoming/{{ distribution }}/{{ codename }}
  when: format == "deb"
