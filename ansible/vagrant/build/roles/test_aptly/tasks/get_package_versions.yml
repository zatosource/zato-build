---

- name: Prepare additional variables
  script: get_zato_version.py

- name: Fetch new vars files
  fetch: src=/home/vagrant/{{ item }}
         dest={{ project_root }}/roles/test_aptly/vars/ flat=yes
  with_items:
      - latest.yml
      - previous.yml
