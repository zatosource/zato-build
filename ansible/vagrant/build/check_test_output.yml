---

- hosts: localhost

  tasks:

    - name: Run check_test_results script
      shell: python check_test_results.py
      register: command_result
      failed_when: "'FAILURE!' in command_result.stderr"
