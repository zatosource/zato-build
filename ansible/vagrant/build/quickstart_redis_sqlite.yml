---

- hosts: '{{ hostname }}'
  become: True
  vars_files:
      - vars/vars.yml
      - vars/common.yml
      - vars/build-box.yml

  roles:

    - install_additional_soft
    - install_redis
    - install_zato
    - create_quickstart
    - test_quickstart
