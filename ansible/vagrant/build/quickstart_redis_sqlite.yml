---

- hosts: '{{ hostname }}'
  become: True
  vars_files:
    - vars/package.yml
    - vars/build-box.yml
    - vars/repository.yml

  roles:

    - install_additional_soft
    - install_redis
    - prepare_install_zato
    - install_zato
    - create_quickstart
    - test_quickstart
