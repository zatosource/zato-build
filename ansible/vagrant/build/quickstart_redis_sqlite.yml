---

    - hosts: '{{ host }}'
      become: True
      vars_files:
          - vars/vars.yml
          - vars/parameters.yml

      roles:

        - install_additional_soft
        - install_redis
        - install_zato
        - create_quickstart
        - test_quickstart
