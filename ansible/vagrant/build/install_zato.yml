---

    - hosts: "{{ hostname }}"
      become: True
      vars_files:
          - vars/vars.yml
          - vars/common.yml
          - vars/build-box.yml

      roles:

          - prepare_install_zato
          - install_zato
