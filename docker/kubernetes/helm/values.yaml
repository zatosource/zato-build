# Default values for  zatohelm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
namespace: "zato"
imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""
image:
  repository: registry.gitlab.com/zatosource/docker-registry/cloud
  pullPolicy: IfNotPresent

env:
  - name: LB_PORT
    value: "80"
  - name: LB_AGENT_PORT
    value: "20151"
  - name: CLUSTER_NAME
    value: "zato"
  - name: SECRET_KEY
    value: "hS4KVhIMPUGxoS1M6xYLq1UW0_lZOFe5eDoRs9Q9Hy0="
  - name: JWT_SECRET_KEY
    value: "p5m2wJxDF3xxFrZLdDu8m15fRUak4eZDPJl5Bfz_wm8="
  - name: ZATO_WEB_ADMIN_PASSWORD
    value: "zato"
  - name: ZATO_IDE_PUBLISHER_PASSWORD
    value: "zato"
  - name: ZATO_ADMIN_INVOKE_PASSWORD
    value: "c97f1aa4-8a20-441e-b46d-d37711a06afc"
  - name: REDIS_HOSTNAME
    value: "redis.zato.svc.cluster.local"
  - name: REDIS_PORT
    value: {{ .Values.redis.service.port }}
  - name: ODB_TYPE
    value: "postgresql"
  - name: ODB_HOSTNAME
    value: "postgres.zato.svc.cluster.local"
  - name: ODB_PORT
    value: {{ .Values.postgresql.service.port }}
  - name: ODB_NAME
    value: "zato"
  - name: ODB_USERNAME
    value: "zato"
  - name: ODB_PASSWORD
    value: "zato"

postgresql:
  replicaCount: 1
  fullname: postgres
  image:
    repository: postgres:11.2
    pullPolicy: IfNotPresent
  replicaCount: 1
  selectorLabels:
      app: postgres
  env:
    - name: POSTGRES_USER
      value: zato
    - name: POSTGRES_PASSWORD
      value: zato
  podSecurityContext: {}
    # fsGroup: 2000
  securityContext: {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000
  service:
    type: LoadBalancer
    port: 5432
  resources: {}
    # We usually recommend not to specify default resources and to leave this as a conscious
    # choice for the user. This also increases chances charts run on environments with little
    # resources, such as Minikube. If you do want to specify resources, uncomment the following
    # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
    # limits:
    #   cpu: 100m
    #   memory: 128Mi
    # requests:
    #   cpu: 100m
    #   memory: 128Mi
  nodeSelector: {}
  tolerations: []
  affinity: {}

redis:
  replicaCount: 1
  fullname: redis
  image:
    repository: redis:5.0.4
    pullPolicy: IfNotPresent
  replicaCount: 1
  selectorLabels:
      app: redis
  podSecurityContext: {}
    # fsGroup: 2000
  securityContext: {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000
  service:
    type: LoadBalancer
    port: 6379
  resources: {}
    # We usually recommend not to specify default resources and to leave this as a conscious
    # choice for the user. This also increases chances charts run on environments with little
    # resources, such as Minikube. If you do want to specify resources, uncomment the following
    # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
    # limits:
    #   cpu: 100m
    #   memory: 128Mi
    # requests:
    #   cpu: 100m
    #   memory: 128Mi
  nodeSelector: {}
  tolerations: []
  affinity: {}

zatoscheduler:
  replicaCount: 1
  fullname: scheduler
  replicaCount: 1
  selectorLabels:
      app: scheduler
  podSecurityContext: {}
    # fsGroup: 2000
  securityContext: {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000
  resources: {}
    # We usually recommend not to specify default resources and to leave this as a conscious
    # choice for the user. This also increases chances charts run on environments with little
    # resources, such as Minikube. If you do want to specify resources, uncomment the following
    # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
    # limits:
    #   cpu: 100m
    #   memory: 128Mi
    # requests:
    #   cpu: 100m
    #   memory: 128Mi
  nodeSelector: {}
  tolerations: []
  affinity: {}
  env:
    - name: ZATO_POSITION
      value: "scheduler"
    - name: LB_HOSTNAME
      value: "server.zato.svc.cluster.local"
    - name: LB_PORT
      value: "80"
    - name: LB_AGENT_PORT
      value: "20151"

zatobootstrap:
  replicaCount: 1
  fullname: bootstrap
  replicaCount: 1
  selectorLabels:
      app: bootstrap
  podSecurityContext: {}
    # fsGroup: 2000
  securityContext: {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000
  resources: {}
    # We usually recommend not to specify default resources and to leave this as a conscious
    # choice for the user. This also increases chances charts run on environments with little
    # resources, such as Minikube. If you do want to specify resources, uncomment the following
    # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
    # limits:
    #   cpu: 100m
    #   memory: 128Mi
    # requests:
    #   cpu: 100m
    #   memory: 128Mi
  nodeSelector: {}
  tolerations: []
  affinity: {}

zatowebadmin:
  replicaCount: 1
  fullname: webadmin
  replicaCount: 1
  selectorLabels:
      app: webadmin
  podSecurityContext: {}
    # fsGroup: 2000
  securityContext: {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000
  service:
    name: web
    type: LoadBalancer
    port: 8183
  resources: {}
    # We usually recommend not to specify default resources and to leave this as a conscious
    # choice for the user. This also increases chances charts run on environments with little
    # resources, such as Minikube. If you do want to specify resources, uncomment the following
    # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
    # limits:
    #   cpu: 100m
    #   memory: 128Mi
    # requests:
    #   cpu: 100m
    #   memory: 128Mi
  nodeSelector: {}
  tolerations: []
  affinity: {}

zatoserver:
  replicaCount: 1
  fullname: server
  replicaCount: 1
  selectorLabels:
      app: server
  podSecurityContext: {}
    # fsGroup: 2000
  securityContext: {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000
  service:
    name: server
    type: LoadBalancer
    port: 17010
  resources: {}
    # We usually recommend not to specify default resources and to leave this as a conscious
    # choice for the user. This also increases chances charts run on environments with little
    # resources, such as Minikube. If you do want to specify resources, uncomment the following
    # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
    # limits:
    #   cpu: 100m
    #   memory: 128Mi
    # requests:
    #   cpu: 100m
    #   memory: 128Mi
  nodeSelector: {}
  tolerations: []
  affinity: {}

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: zatoServiceAccount

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths: []
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local
